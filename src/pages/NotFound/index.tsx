import React, { useEffect, useState } from 'react';
import { NotFoundWrapper } from './styles';

const NotFound = (): JSX.Element => {
    return (<NotFoundWrapper> NotFound page </NotFoundWrapper>)
};

export default NotFound;